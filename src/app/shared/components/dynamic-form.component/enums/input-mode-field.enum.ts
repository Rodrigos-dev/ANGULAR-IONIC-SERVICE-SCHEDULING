export enum EInputModeField {
  TEXT = 'text',
  PASSWORD = 'password',
  EMAIL = 'email',
  NUMBER = 'number',
  SEARCH = 'search',
  TEL = 'tel',
  URL = 'url',
}

export enum EFormatDateValueInInput {
  DATE = 'dd/MM/yyyy',
  TIME = 'HH:mm',
  DATE_TIME = 'dd/MM/yyyy HH:mm',
}
